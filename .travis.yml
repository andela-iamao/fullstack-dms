language: node_js

node_js:
  - node
  - 6
  
services:
  - postgresql

before_script:
  - psql -c 'create database fullstack_dms_development;' -U postgres
  - npm run db:migrate:test

after_script:
  - node node_modules/codeclimate-test-reporter/bin/codeclimate < coverage/lcov.info
